/**
 * Testing route for the webGL rendering without being fully coupled to the chart.
 *
 * Tests that a single point renders as a colored status correctly.
 */
export declare class SingleColoredStatus {
    el: HTMLElement;
    componentDidLoad(): void;
    render(): any;
}
