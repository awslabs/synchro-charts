/**
 * Testing route for the webGL rendering without being fully coupled to the chart.
 *
 * Tests that a single point renders as a colored bar correctly.
 */
export declare class ScSingleColoredBar {
    el: HTMLElement;
    componentDidLoad(): void;
    render(): any;
}
