/**
 * Testing route for the webGL rendering without being fully coupled to the chart.
 *
 * Tests that multiple points renders as multiple bar correctly.
 */
export declare class ScMultipleBars {
    el: HTMLElement;
    componentDidLoad(): void;
    render(): any;
}
