import { JSX } from '../../../stencil-public-runtime';
import { SizeConfig } from '../../../utils/dataTypes';
import { LegendConfig } from '../common/types';
export declare const ChartLegendContainer: ({ legendHeight, config, size: { height, marginRight, marginLeft, marginBottom, marginTop, width }, }: {
    legendHeight: number;
    config: LegendConfig;
    size: SizeConfig;
}, children: JSX.Element) => any;
