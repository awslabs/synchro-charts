import { JSX } from '../../../stencil-public-runtime';
import { SizeConfig } from '../../../utils/dataTypes';
export declare const DataContainer: ({ size: { width, height, marginLeft, marginTop } }: {
    size: SizeConfig;
}, children: JSX.Element) => any;
