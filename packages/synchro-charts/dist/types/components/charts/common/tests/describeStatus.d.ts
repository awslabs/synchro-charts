import { ConnectedComponentPageGenerator } from './types';
export declare const describeStatus: (pageGenerator: ConnectedComponentPageGenerator, tag: string, options?: {
    errors?: boolean;
    loading?: boolean;
}) => void;
