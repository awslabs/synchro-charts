import { MinimalViewPortConfig } from './dataTypes';
export declare const viewPortStartDate: ({ start, end, duration }: MinimalViewPortConfig) => Date;
export declare const viewPortEndDate: ({ start, end, duration }: MinimalViewPortConfig) => Date;
