import { DefaultErrorMessages, DefaultTooltipMessages, loading } from '../../common/constants';
import { DialMessages } from './type';

export const DefaultDialMessages: DialMessages = {
  error: DefaultErrorMessages,
  loading,
  tooltip: DefaultTooltipMessages,
};
